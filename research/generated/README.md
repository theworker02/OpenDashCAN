# Regenerable DBC extract artifacts

Files here (`dbc_signals.csv`, `dbc_messages.csv`, `dbc_inventory.json`) are generated by research/import tools and may be recreated after clone.

Regenerate from repo tooling when needed (see `tools/import_opendbc_honda.py` / donor ingest scripts).
